# Application URLs
# Configure these with YOUR test application URLs
BASE_URL=https://your-app-ui.example.com/
LOGIN_URL=https://your-keycloak.example.com/realms/your-realm/protocol/openid-connect/auth

# Test Credentials
# Configure with YOUR test user credentials (not production users!)
TEST_USER_EMAIL=your-test-user@your-domain.com
TEST_USER_PASSWORD=your-secure-test-password

# Optional: Timeouts (milliseconds)
# Defaults: TIMEOUT=60000, NAVIGATION_TIMEOUT=90000, SHORT_TIMEOUT=10000
TIMEOUT=60000
NAVIGATION_TIMEOUT=90000
SHORT_TIMEOUT=10000

# Security Note: Never commit .env file to version control
# Always use .env for local development only
# For CI/CD pipelines, use GitHub Secrets or similar secure methods
